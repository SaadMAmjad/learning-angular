import { Asn1DirectiveDirective } from './asn-1-directive.directive';

describe('Asn1DirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new Asn1DirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
